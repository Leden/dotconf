#!/bin/sh

set -x

DIR="$HOME/cfg"

echo "Installing apt packages..."
cat "${DIR}/apt-packages" | xargs sudo apt install -y

echo "Installing snap packages..."
cat "${DIR}/snap-packages" | xargs sudo snap install --classic

echo "Installing npm packages..."
cat "${DIR}/npm-packages" | xargs sudo npm install -g
